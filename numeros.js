const numeros = [6, 14, 7, 40, 17, 4, 5, 44, 13, 8];
for (let i = 0; i < numeros.length; i++) {
    // ver se o numero é par
    if (numeros[i] % 2 === 0) {
        // se for par imprimir
        console.log(numeros[i]);
  }
}